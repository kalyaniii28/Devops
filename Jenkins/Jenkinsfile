pipeline {
    agent { label 'python-3.9-agent' }  // Use a label for a node with Python 3.9 installed

    stages {
        stage('Test') {
            steps {
                sh 'python3 add.py'  // Use python3 to ensure Python 3.9 is used
            }
        }
    }
}