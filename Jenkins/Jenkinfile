pipeline {
  agent {
    docker 'python:3.9'
  }
  stages {
        // Stage to checkout the Git repository containing the script
        stage('Checkout') {
            steps {
                git 'https://github.com/kalyaniii28/Devops.git'  // Replace with your Git repo
            }
        }
  stages {
    stage('Run python function') {
      steps {
        sh 'python add.py'
      }
    }
  }
  }
  post {
    success {
      echo " successful"
    }
    failed {
      echo " failed build"
    }
  }
  }
}

