terraform {
  backend "s3" {
    bucket = "<account_id>-terraform-state" 
    key    = "Path to s3 back where you want to save state fiel"
    region = "ap-southeast-2"
    dynamodb_table = "terraform-lock"
    encrypt = true
  }
}
